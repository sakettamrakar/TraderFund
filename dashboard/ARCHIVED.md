# ⚠️ ARCHIVED — Legacy Dashboard

This folder contains the **original static HTML/JS dashboard** and design spec documents.

**It is no longer the active dashboard.** All new dashboard work targets:

- **Backend:** `src/dashboard/backend/` (FastAPI)
- **Frontend:** `src/dashboard/frontend/` (React + Vite)

The files here are retained as read-only reference material.
Do not add new features or fix bugs in this folder.
